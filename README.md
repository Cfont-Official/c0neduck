# DuckDuckGo Proxy

A lightweight Node.js proxy that allows DuckDuckGo to run inside an iframe by removing CSP and frame-blocking headers.

## 🚀 Deployment

### Local
```bash
npm install
npm start
```

### Render
- Push to GitHub
- Create new Web Service on Render
- Start command: `npm start`

### Vercel
- Import project to Vercel
- It will auto-detect Node and deploy
